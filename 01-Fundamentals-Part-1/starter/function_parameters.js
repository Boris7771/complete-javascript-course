function operations(x, y) {
  console.log("this are numbers", x, y);
  console.log("sum", x + y);
  console.log("minus", x - y);
  console.log("multiply", x * y);
  console.log("divide", x / y);
  console.log("modulo", x % y);
  console.log("exponent", x ** y);
}
operations(1, 0);
operations(4, 6);
operations(2, 500);
operations(2000, 2);
